@using Codex.Web.Components.Layout

<nav class="sidebar">
    <div class="d-flex align-items-center gap-3 mb-5 px-3">
        <div style="width: 32px; height: 32px; background: linear-gradient(135deg, var(--accent-color) 0%, #106ebe 100%); border-radius: 8px; display: grid; place-items: center; box-shadow: 0 4px 12px var(--accent-glow);">
            <LucideIcon IconName="scroll" Size="32" Class="text-white" />
        </div>
        <span style="font-weight: 800; font-size: 1.15rem; letter-spacing: 0.05em; text-transform: uppercase; background: linear-gradient(to right, #ffffff, #a1a1a1); -webkit-background-clip: text; -webkit-text-fill-color: transparent;">TTRPG.Codex</span>
    </div>

    <div class="nav-section-label">Library</div>
    <NavLink href="" class="nav-link" Match="NavLinkMatch.All">
        <LucideIcon IconName="layout-grid" Size="18" />
        <span>Overview</span>
    </NavLink>
    <AuthorizeView>
        <Authorized>
            <NavLink href="campaigns" class="nav-link">
                <LucideIcon IconName="map" Size="18" />
                <span>Campaigns</span>
            </NavLink>
        </Authorized>
    </AuthorizeView>
    <NavLink href="grimoire" class="nav-link">
        <LucideIcon IconName="book-open" Size="18" />
        <span>Grimoire</span>
    </NavLink>


    <div class="nav-section-label">System</div>
    <NavLink href="players" class="nav-link">
        <LucideIcon IconName="users" Size="18" />
        <span>Players</span>
    </NavLink>
    <AuthorizeView>
        <Authorized>
            <NavLink href="settings" class="nav-link">
                <LucideIcon IconName="settings" Size="18" />
                <span>Configuration</span>
            </NavLink>
        </Authorized>
        <NotAuthorized>
            <div class="mt-auto">
                <a href="/login" class="btn-accent w-100 justify-content-center text-decoration-none mx-3 mb-4">
                    <LucideIcon IconName="log-in" Size="18" />
                    <span>Enter Grimoire</span>
                </a>
            </div>
        </NotAuthorized>
    </AuthorizeView>

    <div class="mt-auto px-3 pb-4">
        <AuthorizeView>
            <Authorized>
                @{
                    var email = context.User.FindFirst(System.Security.Claims.ClaimTypes.Email)?.Value;
                    var username = context.User.Identity?.Name ?? "User";
                    var gravatarUrl = string.IsNullOrEmpty(email)
                        ? $"https://ui-avatars.com/api/?name={Uri.EscapeDataString(username)}&background=0D8ABC&color=fff&rounded=true"
                        : $"https://www.gravatar.com/avatar/{GetMd5Hash(email.Trim().ToLowerInvariant())}?d=identicon&s=40";
                }
                <div class="d-flex align-items-center gap-3 p-3 rounded" style="background-color: var(--fluent-surface-secondary); border: 1px solid var(--fluent-border); box-shadow: 0 4px 6px rgba(0,0,0,0.1);">
                    <img src="@gravatarUrl" alt="User Avatar" class="rounded-circle" style="width: 36px; height: 36px; object-fit: cover; border: 2px solid var(--fluent-border-strong);" />
                    <div class="d-flex flex-column text-truncate flex-grow-1">
                        <span class="small fw-semibold text-truncate" style="color: var(--text-primary);">@username</span>
                        <a href="/logout" class="text-danger small text-decoration-none opacity-75 hover-opacity-100" style="font-size: 0.75rem; transition: var(--transition-base);">Sign Out</a>
                    </div>
                </div>
            </Authorized>
        </AuthorizeView>
    </div>

</nav>

@code {
    private static string GetMd5Hash(string input)
    {
        using (var md5 = System.Security.Cryptography.MD5.Create())
        {
            var bytes = System.Text.Encoding.UTF8.GetBytes(input);
            var hash = md5.ComputeHash(bytes);
            return BitConverter.ToString(hash).Replace("-", "").ToLowerInvariant();
        }
    }
}
